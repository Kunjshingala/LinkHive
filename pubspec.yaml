name: link_hive
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.10.1
  flutter: 3.38.3

dependencies:
  flutter:
    sdk: flutter

  go_router: any
  rxdart: any
  firebase_core: any
  receive_sharing_intent: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: any

flutter:
  uses-material-design: true

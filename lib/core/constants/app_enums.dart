enum AccountItem { language, syncData, deleteLocalData, theme, helpFeedback, auth }

/// Priority levels for a saved link.
enum LinkPriority { high, normal, low }

/// Button style variant for [NeoBrutalistButton].
enum ButtonVariant {
  /// Surface fill, distinct colored gap shadow — the primary action button.
  primary,

  /// Surface fill, surface gap shadow — secondary/outlined action.
  outlined,
}
